Django>=5.2,<6
Pillow>=12.0
psycopg2-binary>=2.9
python-dotenv>=1.2
gunicorn>=23.0
whitenoise>=6.7
django-allauth>=65.0
# django-allauth la necesita para verificar el id_token OIDC que manda
# Google al iniciar sesión — sin ella, el callback de "Continuar con
# Google" falla con ModuleNotFoundError (no aparece como dependencia
# directa de allauth en algunas versiones).
PyJWT>=2.9
requests>=2.32
# Solo entra en juego si hay credenciales de Backblaze B2 en el entorno
# (ver config/settings/produccion.py) — despliegue gratuito sin disco
# persistente (Render), donde las fotos no pueden vivir en el sistema de
# archivos del contenedor. En el despliegue con Docker/VPS (disco propio)
# se instala igual pero nunca se usa.
django-storages[s3]>=1.14
# Genera el QR de la página de descarga del APK (config/views.py) sin
# depender de un servicio externo — importa por conexión intermitente en
# campo, no tiene sentido que bajar la app dependa de un tercero en pie.
# El backend SVG no necesita Pillow.
qrcode>=8.0
